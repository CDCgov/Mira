
x-mira-image:
  &mira-image
  cdcgov/mira:latest

x-data-volume:
  &data-volume
  type: bind
  source: /path/to/data
  target: /data

services:
  mira:
    container_name: mira
    image: *mira-image
    restart: always
    ports:
      - 8020:8050
    volumes:
      - *data-volume
